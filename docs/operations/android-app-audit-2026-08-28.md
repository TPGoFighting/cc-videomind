# Teach Player Android 测试与修复报告

日期：2026-08-28
范围：`apps/mobile` Android 客户端、移动端 API 调用、UI 排版、交互、可访问性、缓存和异常恢复。
设备：实体 Android 手机 `RMX6699`，设备序列号 `3B15BT00NX600000`，arm64-v8a，Android SDK 36。

## 证据标记

- **真机复现**：在本轮代码修改前，已通过同一台实体手机操作或查看日志确认。
- **代码/构建验证**：通过源代码检查、类型检查、单元测试、Expo 检查或 Android 构建确认。
- **修复后真机证据**：设备重新连接后已安装 APK，并完成首页、设置、语料库、单词本、句子本、笔记本和 3 个指定视频的最终点测。

## 问题与处理结果

| # | 问题与影响 | 处理 | 证据 / 状态 |
|---:|---|---|---|
| 1 | 设置首页同时出现 Expo Router 原生标题和自绘安全区，顶部出现大块空白，排版像多了一层导航栏。 | 关闭 settings index 的原生 header；ScrollView 关闭重复 inset，并保留 SafeAreaView。 | 真机复现；修复后真机截图确认；已修复 |
| 2 | 登录、注册、设置、管理页的系统安全区处理方式不统一，部分页面可能出现双顶部间距。 | 统一显式使用 `contentInsetAdjustmentBehavior="never"`，由页面 SafeAreaView 负责边距。 | 代码/构建验证；已修复 |
| 3 | Android 键盘弹出时登录、注册、创建页没有明确的高度收缩策略，输入框可能被键盘遮挡。 | Android KeyboardAvoidingView 改为 `behavior="height"`，iOS 保留 padding。 | 代码/构建验证；已修复 |
| 4 | access token 失效后，用户状态可能停留在旧账号，页面继续使用过期会话。 | `refreshProfile` 捕获 401，清除用户、token、管理员和 tier 状态并退出会话。 | 代码/构建验证；已修复 |
| 5 | 历史记录查询 key 没有绑定账号，切换账号后可能看到上一个账号的缓存。 | history query key 加入 `accessToken`。 | 代码/构建验证；已修复 |
| 6 | 单词本查询 key 没有绑定账号，账号切换存在数据串看风险。 | vocabulary query key 加入 `accessToken`。 | 代码/构建验证；已修复 |
| 7 | 笔记 API 失败时只显示空白或普通空状态，用户无法判断是没有笔记还是请求失败。 | 增加明确错误 EmptyState 和“重试”按钮。 | 代码/构建验证；已修复 |
| 8 | 订阅状态接口错误被吞掉，页面缺少恢复操作。 | 保留真实错误信息，加入 48dp 重试按钮，并同步更新 query invalidation key。 | 代码/构建验证；已修复 |
| 9 | 管理员 AI 配置、视频、支付页面的 query key 没有绑定会话或筛选条件，切换管理员状态后数据可能陈旧。 | 分别将 token 和必要的 filter 纳入 query key。 | 代码/构建验证；已修复 |
| 10 | 注销点击后先跳转，异步清理还没有完成，可能短暂回到仍有权限的页面。 | 注销回调改为先等待 `signOut`，成功后再替换路由；失败时显示提示。 | 代码/构建验证；已修复 |
| 11 | 视频详情页轮询当前播放时间未随路由失焦停止，切换页面后仍会持续执行。 | 改用 `useFocusEffect` 管理轮询的启动和清理。 | 代码/构建验证；已修复 |
| 12 | 翻译结果每个片段变化都立即写本地存储，长字幕会造成高频 I/O 和卡顿风险。 | 翻译持久化增加 250ms debounce，并在卸载时 flush 最新结果。 | 代码/构建验证；已修复 |
| 13 | 语法卡片此前使用固定的本地 mock 结果，与用户点击的句子无关。 | 新增真实 `/api/grammar-analysis`，将句子传给 AI provider，返回结构化 schema。 | 代码/构建验证；已修复 |
| 14 | 语法分析没有服务端缓存，重复点击同一句会重复消耗模型调用。 | 增加 24 小时、500 条 LRU 内存缓存，并用单飞合并并发请求。 | 代码/构建验证；已修复 |
| 15 | 语法分析失败后只有失败文案，缺少可恢复入口。 | 增加带真实 API 重试的按钮；React Query 保留一次有限自动重试。 | 代码/构建验证；已修复 |
| 16 | 字幕翻译遇到分页响应或连接中断时会停在半成品，用户必须手动点击“继续翻译”。 | 复用共享 polling policy，识别 pending segments，自动退避轮询；部分 SSE 结果也会继续处理。 | 修复后真机看到后台逐页生成和自动重试；260 项测试覆盖中断和未完成分段；已修复 |
| 17 | 长字幕 `FlatList.scrollToIndex` 超出测量范围时会触发失败，自动跟随可能失效。 | 加入 `onScrollToIndexFailed`，先 offset 定位，再延迟重试 index。 | 代码/构建验证；已修复 |
| 18 | 当前字幕行每次变化都触发 haptic，连续播放时震动过密，干扰学习。 | 删除自动字幕切换 haptic，只保留用户主动操作反馈。 | 代码/构建验证；已修复 |
| 19 | 保存单词后只失效旧的 `vocabulary` key，单词本页面使用的新 key 不会立即更新。 | 统一失效 `user-vocabulary` query key。 | 代码/构建验证；已修复 |
| 20 | 新用户的单词、句子、段落、短语页面预置大批 mock 个人数据，用户会误认为这些是自己的学习记录。 | 个人语料默认改为空数组；练习页保留最小安全 fallback，保证页面仍可使用。 | 代码/构建验证；已修复 |
| 21 | 要点、摘要、问答、笔记面板混用英文 tab 和状态文案，中文用户理解成本高。 | 面板 tab、空状态、错误、聊天和笔记操作统一为中文。 | 真机旧版本可见英文 UI；代码已修复 |
| 22 | 本地上传没有有效时长时仍发送 `duration=0`，服务端可能按零时长切分 ASR 字幕。 | 客户端只有在时长大于 0 时发送字段；服务端对缺失或非法时长使用 60 秒兜底。 | 代码/构建验证；已修复 |
| 23 | 分享卡片没有缩略图时仍渲染 Image，容易出现破图或空白块。 | 缩略图缺失时显示 TP fallback block，同时传入真实视频标题和缩略图。 | 代码/构建验证；已修复 |
| 24 | 视频播放器把本地流 URL 直接插入 HTML/脚本，特殊字符可能破坏脚本或导致加载异常。 | 使用 JSON 编码并转义 `<`、U+2028、U+2029，再由脚本设置 video src。 | 代码/构建验证；已修复 |
| 25 | 本地 WebView 允许任意导航，外部链接可脱离播放器边界打开。 | 仅允许 about/data/file/content、本地 stream URL 和 API base URL。 | 代码/构建验证；已修复 |
| 26 | 切换视频时播放器 ready、pending、错误和 WebView 引用未完全重置，可能沿用上一个视频的状态。 | 以 videoId 为边界重置播放器 refs、状态、错误和重试次数。 | 代码/构建验证；已修复 |
| 27 | 多个图标和文字操作只有 40–44dp，触控面积偏小，不符合 Android 常用 48dp 目标。 | IconButton、页面返回、筛选、清除、分享、面板操作等交互目标统一到至少 48dp。 | 代码/构建验证；已修复 |
| 28 | 练习选项缺少明确的 role、选中态和禁用态，读屏用户无法判断当前选择。 | 增加 button role、选项标签、selected、disabled 和 56dp 高度。 | 代码/构建验证；已修复 |
| 29 | 听力练习播放按钮没有明确的可访问名称和忙碌状态。 | 增加播放按钮 label、button role 和 busy 状态。 | 代码/构建验证；已修复 |
| 30 | 闪卡翻面和评分按钮缺少读屏标签，且部分操作区域太小。 | 增加 flip/rating 语义标签，操作区域统一到至少 48dp。 | 代码/构建验证；已修复 |
| 31 | 解析任务行的删除按钮可能冒泡触发行点击，用户点击删除后还会打开任务。 | 删除按钮停止事件传播，并补齐 disabled、busy、label。 | 代码/构建验证；已修复 |
| 32 | 首页下拉刷新连接的是静态 mock feed，刷新不会产生实际结果，用户会误以为请求失败。 | 移除无效 refreshing/onRefresh 控制，保留真实的静态 feed 呈现。 | 代码/构建验证；已修复 |
| 33 | 语法弹窗关闭按钮使用英文 accessibility label，中文读屏体验不一致。 | 改为中文关闭标签并扩展到 48dp。 | 代码/构建验证；已修复 |
| 34 | 个人资料显示名允许保存纯空白内容，密码输入提示与实际最低长度不一致。 | 保存前 trim 并禁止空值；密码输入统一提示至少 8 位，补齐输入语义。 | 代码/构建验证；已修复 |
| 35 | 笔记本页返回按钮右侧的 JSX 行内注释被编译成一个裸空格字符串，Android 开发运行时触发 `Text strings must be rendered within a <Text>`。 | 移除该行内注释，避免 JSX 生成裸文本节点。 | 真机复现；热更新后错误提示消失；最终日志无该错误；已修复 |
| 36 | 真机网络瞬断时 YouTube iframe API 曾返回 `ERR_CONNECTION_RESET`，播放器表现为黑屏，用户无法判断是视频、WebView 还是字幕服务故障。 | 保留播放器已有的 videoId 状态重置、加载重试和导航隔离；网络恢复后重新点测 3 个视频，并让字幕翻译继续走自动轮询。 | 用户确认黑屏已恢复；`kG8AoExkX40`、`QhQCJkodH_o`、`v4F1gFy-hqg` 均有真实视频画面；已解决 |

## 自动化与构建结果

以下检查已完成：

1. 根项目 `npm run typecheck`：通过。
2. `apps/mobile` `npm run typecheck`：通过。
3. 根项目 `npm test`：260 tests、45 suites，260 passed、0 failed。
4. `npx expo-doctor`：18/18 checks passed。
5. `npx expo export --platform android`：成功，Android bundle 约 6.97 MB。
6. `./gradlew :app:assembleDebug`：成功，使用 JDK 17；arm64 APK 已生成。

## 真机收尾状态

设备重新连接后已安装 `com.teachplayer.app` `versionName=1.8.1`、`versionCode=9` 的 arm64 APK。最终真机已确认首页、设置顶部排版、语料库、单词本、句子本、笔记本和视频详情页可进入；3 个指定视频均显示真实画面，字幕卡片和双语切换可见。`v4F1gFy-hqg` 首次显示翻译连接重试，等待后自动出现英文与中文译文卡片，证明部分结果可先返回并由轮询继续处理。

复测前清空 Android 日志，最终检查未发现 `FATAL EXCEPTION`、`ReactNativeJS`、`Unhandled`、`TypeError`、`Text strings must be rendered` 或 `AndroidRuntime`。当前代码、自动化检查、APK 构建和修复后真机回归均已完成。
