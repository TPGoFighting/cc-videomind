# T05 首个 10 分钟价值闭环 — 本地实现证据

日期：2026-07-22
状态：本地代码、自动化契约与 1440px/390px 浏览器双流程验收完成；真实账号续存和真实新用户 10 分钟完成率仍是发布门禁。

## 已实现的闭环

1. 当前 Taste 首页工作树已经提供稳定 TED 示例的一键入口；用户也可继续粘贴公开 YouTube 链接。
2. 工作台明确展示“读取字幕（通常 5–30 秒）→ 提炼要点（约 30–120 秒）→ 开始学习”。
3. 字幕先成功时立即保留；AI 解析失败不会清除字幕，并提供独立重试。
4. 第一处字幕收藏区域解释：句子保存原文/视频/时间点，单词写入次日（约 24 小时后）复习计划；同时显示是否已保存并提供独立“复制原文”。
5. 匿名用户先体验，点击收藏时才进入登录。学习项保存在 `sessionStorage`，不会进入 URL；登录返回后仅恢复同一视频、30 分钟内且通过 Zod schema 的项目。
6. transcript、analysis、translation、chat 的技术错误码统一映射为用户可理解的原因与恢复动作，前端不直接暴露供应商错误正文。

## 自动化边界

- 成功视觉状态：`/video/eIho2S0ZahI?fixture=ready`
- 字幕失败状态：`/video/eIho2S0ZahI?fixture=failure`
- 访客保存状态：`/video/eIho2S0ZahI?fixture=ready&fixtureSave=guest`
- fixture 仅在非生产环境启用，不加载播放器，不调用字幕、词典、AI、支付、生产账户或生产数据。
- `fixtureSave=guest` 只复用确定性字幕数据并走真实的“暂存 → 登录”前端分支；生产环境会忽略所有 fixture 参数。
- 待保存学习项不会出现在登录返回 URL；解析器会拒绝畸形、过期或来自未来的项目。

## 浏览器验收证据

- 1440×900 成功态：页面身份为 `视频学习工作区 | Teach Player`；三阶段进度、首个收藏引导、四组复制/收藏动作均可访问，页面水平溢出为 0。
- 390×844 成功态：复制与收藏按钮各 4 个，所有可见按钮均为 44px 高；复制后出现“已复制”，收藏后出现“已收藏”与可返回原视频时间点的确认文案，水平溢出为 0。
- 1440×900 与 390×844 字幕失败态：均展示“没有可用字幕”、具体原因、“选择其他视频”和“在 YouTube 检查”；移动端两个恢复入口均为 44px 高，水平溢出为 0。
- 字幕行已从嵌套交互控件改成独立的时间跳转、正文跳转、复制和收藏按钮；DOM 检查得到嵌套 button/role-button 数量 0，每个跳转按钮都有包含时间点的可读名称。
- 访客点击收藏后到达 `/login?next=%2Fvideo%2FeIho2S0ZahI%3Fresume%3Dsave`，登录页明确说明会返回刚才的学习位置；URL 不含字幕正文。该跳转只会在 `sessionStorage.setItem` 成功后执行。
- 首页存在无需先输入 URL 的“用 TED 演讲试用”入口，目标为已核对视频 `/video/eIho2S0ZahI`。

## 本地质量证据

- `npm run lint`：通过，0 warning。
- `npm run typecheck`：通过。
- `npm test`：161 项通过，0 失败。
- `npm run build`：通过，55 个静态页面生成，视频工作台保持动态路由。
- `git diff --check`：通过。

## 尚未作为通过项计入

- 非本地模式的真实“收藏 → 登录/注册 → 返回 → 自动续存”账户流程。
- 至少一位全新目标用户在 10 分钟内完成首次保存的计时证据。
- 首页 Taste 实验文件仍属于 T00 前既有未提交工作，本任务未将其混入提交。
